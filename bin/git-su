#!/bin/sh

# git-su
#
# Copyright (c) 2013 - 2014, Andrew Keller
#
# Updates and/or initializes all submodules recursively.

cd "$(git rev-parse --show-toplevel)" || exit $?

git repocache-submodule update --init --recursive || exit $?

git submodule foreach --recursive git disconnect-alternates || exit $?
